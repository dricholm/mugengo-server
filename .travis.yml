dist: trusty

language: node_js
node_js:
  - 'node'

services:
  - postgresql

branches:
  only:
    - master

before_script:
  - psql -c 'create database mugengo_test;' -U postgres
  - cp .env.ci .env

script:
  - npm run ci

after_success:
  - cat coverage/lcov.info | coveralls
